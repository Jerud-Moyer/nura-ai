import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      // main: '#209ac9'
      main: '#21a5d9'
    },
    light: {
      main: '#fbfadd'
    },
    bright_light: {
      main: '#f4dc76',
    },
    blaze: {
      main: '#d66b02'
    }

  },
  breakpoints: {

  }
})

export default theme;
